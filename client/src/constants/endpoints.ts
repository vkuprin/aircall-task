const authNamespace = 'auth';
const signIn = `${authNamespace}/login`;
const refresh = `${authNamespace}/refresh-token`;
const deprecatedRefresh = `${authNamespace}/refresh-token`;

const calls = 'calls';
const currentUser = 'me';

export {
  refresh,
  signIn,
  calls,
  currentUser,
};
